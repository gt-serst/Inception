version: '3'
services:
  mariadb:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mariadb
    ports:
      - "3306:3306"
    env_file:
      - .env
    volumes:
      - mariadb:/var/lib/mysql
    restart: always

volumes:
  mariadb:
    driver: local
    driver _opts:
      type: 'none'
      o: 'bind'
      device: '/home/test/data/mariadb'

